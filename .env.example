# Environment Variables Configuration
# Copy this file to .env and fill in the actual values
# DO NOT commit .env to git!

# ========================================
# JWT Configuration
# ========================================
# REQUIRED: Secret key for JWT token signing (minimum 256 bits)
# Generate a secure key using: openssl rand -base64 32
JWT_SECRET=your-256-bit-secret-key-here-please-change

# Access token expiration time in milliseconds (default: 1 hour)
JWT_EXPIRATION_MS=3600000

# Refresh token expiration time in milliseconds (default: 7 days)
JWT_REFRESH_EXPIRATION_MS=604800000

# ========================================
# Spring Profile Configuration
# ========================================
# dev: H2 in-memory database (local development)
# prod: PostgreSQL (production on Render)
SPRING_PROFILES_ACTIVE=dev

# ========================================
# Database Configuration (Production - PostgreSQL)
# ========================================
# Render automatically provides DATABASE_URL for PostgreSQL
# Format: postgres://user:password@host:port/database
# You don't need to set this manually on Render
# DATABASE_URL=postgres://username:password@localhost:5432/fivepapa_db

# ========================================
# CORS Configuration (Production)
# ========================================
# Allowed origins for CORS (comma-separated)
# CORS_ALLOWED_ORIGINS=https://elvis1056.github.io,https://yourdomain.com
